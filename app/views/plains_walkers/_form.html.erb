<%= form_for @plains_walker, class: "form" do |f| %>
  <% if @plains_walker.errors.any? %>
    <div id="error_explanation">
      <h2
        ><%= pluralize(@plains_walker.errors.count, "error") %> prohibited this plains_walker from being saved:
      </h2>

      <ul>
        <% @plains_walker.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :password %><br>
    <%= f.text_field :password, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :avatarUrl %><br>
    <%= f.text_area :avatarUrl, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
